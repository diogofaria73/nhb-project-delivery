{
  "name": "@nhb-status-report/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Shared types and contracts between frontend and backend",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
